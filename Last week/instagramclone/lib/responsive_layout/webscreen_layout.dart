import 'package:flutter/material.dart';

class WebScreenScreenLayout extends StatelessWidget {
  const WebScreenScreenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('WebScreen'),
      ),
    );
  }
}
